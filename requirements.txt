# Core dependencies
huggingface_hub
pandas
rich
requests

# Web app (pinned for cloud compatibility)
streamlit>=1.31.1
altair>=5.2.0

# Inference (MLX for local Mac, Transformers for Cloud/Linux)
mlx; platform_system == "Darwin"
mlx-vlm; platform_system == "Darwin"
transformers
torch
peft
accelerate
bitsandbytes
